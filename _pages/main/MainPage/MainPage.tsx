import React from 'react'
import PageWrapper from '../../../components/pagesRelated/PageWrapper/PageWrapper'
import { routeNames } from '../../../config/routeNames'

function MainPage() {
	return (
		<PageWrapper header={routeNames.main.name}>
			<p></p>
		</PageWrapper>
	)
}

export default MainPage
